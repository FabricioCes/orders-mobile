<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain> <!-- Permitir localhost -->
    </domain-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">*.*.*.*</domain> <!-- Permitir cualquier IP -->
    </domain-config>
    <base-config cleartextTrafficPermitted="true" /> <!-- Permitir cualquier dominio -->
</network-security-config>