import Tables from '@/components/Tables';
import { View, Text, StyleSheet } from 'react-native';

export default function Tab() {
  return (
    <View>
      <Tables qty={20} place='Comedor'/>
    </View>
  );

}